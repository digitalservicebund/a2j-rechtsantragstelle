pre-commit:
  commands:
    prettier:
      run: node_modules/.bin/prettier --write '{staged_files}' && git add '{staged_files}'
    eslint:
      run: npm run eslint
    typecheck:
      run: npm run typecheck
