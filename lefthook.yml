pre-push:
  parallel: true
  commands:
    "unit tests":
      run: pnpm run test
    lint:
      run: pnpm run lint
    format:
      run: node_modules/.bin/prettier --write '{staged_files}' && git add '{staged_files}'
    typecheck:
      run: pnpm run typecheck
