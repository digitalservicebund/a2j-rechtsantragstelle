pre-commit:
  parallel: true
  commands:
    format:
      run: node_modules/.bin/prettier --write '{staged_files}' && git add '{staged_files}'
    lint:
      run: pnpm run lint
    typecheck:
      run: pnpm run typecheck

pre-push:
  commands:
    "unit tests":
      run: pnpm test
